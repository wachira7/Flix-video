//src/api/routes/social.routes.js
const express = require('express');
const router = express.Router();

// TODO: Implement social routes
router.get('/', (req, res) => {
  res.json({ message: 'Social routes coming soon' });
});

module.exports = router;